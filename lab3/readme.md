#HTTP (Hyper Text Tranfer Protocol) Module
it is built in Node.js Module which allows developers to create web server and web client without installing any external package
one of the most important module in Node.js is, originally designed for building scalable network application and web servers.

1. create web servers
2. handle cliendt req
3. build REST APIs
4. non-blocking I/O
5. foundation of Express.js
